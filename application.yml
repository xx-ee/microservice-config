spring:
  profiles:
    active: 
    - dev

---
spring:
    profiles: dev 
     application:
       name: microservice-config-dev
---
spring:
  profiles: test
  application:
    name: microservice-config-test

#保存为utf-8格式